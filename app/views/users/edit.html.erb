<%# ユーザー情報編集ページ %>
<div class="ag-container">
  <div style="max-width: 600px; margin: 0 auto;">
    <div class="ag-page-header">
      <h2>⚙ プロフィールを編集</h2>
    </div>

    <div class="ag-form-card">
      <%= form_with model: @user do |f| %>
        <%= render 'layouts/error', obj: @user %>

        <div class="ag-form-group">
          <%= f.label :name, "ユーザー名" %>
          <%= f.text_field :name, placeholder: "名前を入力...", class: "form-control" %>
        </div>

        <div class="ag-form-group">
          <%= f.label :profile_image, "プロフィール画像" %>
          <%= f.file_field :profile_image, accept: "image/*", class: "form-control" %>
        </div>

        <div class="ag-form-group">
          <%= f.label :introduction, "自己紹介" %>
          <%= f.text_area :introduction, placeholder: "自己紹介を書いてみましょう...", rows: 4, class: "form-control" %>
        </div>

        <div style="display: flex; gap: 0.75rem; margin-top: 0.5rem;">
          <%= f.submit "💾 保存する", class: "ag-btn ag-btn--primary" %>
          <%= link_to "← 戻る", :back, class: "ag-btn ag-btn--outline" %>
        </div>
      <% end %>
    </div>
  </div>
</div>